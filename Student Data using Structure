#include <iostream>
using namespace std;
struct Student {
    string name;
    int roll;
    int marks;
};
int main() {
    Student s;
    cin >> s.name >> s.roll >> s.marks;
    cout << s.name << " " << s.roll << " " << s.marks << "\n";
    return 0;
}
